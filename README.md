[sdf](https://sdf.gatunes.com/)
==

#### Local development

```bash
# clone this repo
git clone https://github.com/danielesteban/sdf.git
cd sdf
# install dependencies
pnpm install
# start the dev environment:
pnpm start
# open http://localhost:8080/ in your browser
```
